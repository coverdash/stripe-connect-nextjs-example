# Stripe Connect OAuth Configuration
# Get these from: https://dashboard.stripe.com/settings/connect

# Client Environment Variables (Platform/Merchant A)
NEXT_PUBLIC_STRIPE_ENVIRONMENT=test
NEXT_PUBLIC_STRIPE_CLIENT_ID=ca_xxxxxxxxxxxxxxxxxxxxx
NEXT_PUBLIC_REDIRECT_URI=/oauth-callback

# Server Environment Variables (Platform/Merchant A)
# Secret key for your platform account
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxx

# For development only - Vercel sets these automatically in production
NEXT_PUBLIC_VERCEL_ENV=development
NEXT_PUBLIC_VERCEL_PROJECT_PRODUCTION_URL=localhost:3000

# Notes:
# 1. Get your client_id from: https://dashboard.stripe.com/settings/connect
# 2. Add redirect URIs in Stripe Dashboard: https://dashboard.stripe.com/settings/connect
#    - For local dev: http://localhost:3000/oauth-callback
#    - For production: https://your-domain.com/oauth-callback
# 3. The STRIPE_SECRET_KEY should be from your platform account (Merchant A)
# 4. Partner accounts (Merchant B) will authenticate via OAuth
