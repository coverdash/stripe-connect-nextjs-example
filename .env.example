# Stripe Connect OAuth Configuration
# Get OAuth settings from: https://dashboard.stripe.com/settings/connect

# Client Environment Variables (Partner app)
NEXT_PUBLIC_STRIPE_ENVIRONMENT=test
NEXT_PUBLIC_PARTNER_STRIPE_CLIENT_ID=ca_xxxxxxxxxxxxxxxxxxxxx
NEXT_PUBLIC_REDIRECT_URI=/oauth-callback

# Server Environment Variables
# Partner secret key: used for OAuth token exchange, cloning, and connected-account direct charges
PARTNER_STRIPE_SECRET_KEY=sk_test_partner_xxxxxxxxxxxxxxxxxxxxx

# Coverdash secret key: used for independent charging without stripeAccount header
COVERDASH_STRIPE_SECRET_KEY=sk_test_coverdash_xxxxxxxxxxxxxxxxxxxxx

# For development only - Vercel sets these automatically in production
NEXT_PUBLIC_VERCEL_ENV=development
NEXT_PUBLIC_VERCEL_PROJECT_PRODUCTION_URL=localhost:3000

# Notes:
# 1. NEXT_PUBLIC_PARTNER_STRIPE_CLIENT_ID should come from the Partner Stripe Connect settings.
# 2. Add redirect URIs in Stripe Dashboard:
#    - Local: http://localhost:3000/oauth-callback
#    - Production: https://your-domain.com/oauth-callback
# 3. PARTNER_STRIPE_SECRET_KEY belongs to the Partner Stripe account (OAuth app owner).
# 4. COVERDASH_STRIPE_SECRET_KEY belongs to the Coverdash Stripe account (connected account owner).
